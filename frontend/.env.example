VITE_API_URL=http://localhost:8000/api/v1
VITE_SITE_URL=https://jobly.com
# Enable WebSocket for notifications and real-time chat (requires Daphne on backend)
VITE_WS_ENABLED=false
# Optional: connect directly to backend WS (bypass Vite proxy), e.g. ws://localhost:8000
# VITE_WS_URL=ws://localhost:8000
